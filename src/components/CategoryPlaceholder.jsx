const CategoryPlaceholder = () => {
    return (
        <div
            key={index}
            className={`w- rounded-lg shadow-md text-left `}
        >
        </div>
    );
};

export default CategoryPlaceholder;

